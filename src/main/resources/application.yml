server:
  port: 8080
spring:
  redis:
    host: localhost
    port: 6379
    password:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      enable-auto-commit: true
      group-id: foo
      auto-offset-reset: earliest